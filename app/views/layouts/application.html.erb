<!DOCTYPE html>
<html>
  <head>
    <title>Catalogo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h1>Catálogo de Bibliotecas Rodrigo de Triana 4235</h1>
    <%= form_with url: "/search", method: :get do |form| %>
      <%= form.label 'Buscar por código de ubicación, título o autor de obra, título o autor de capítulo, tema, idioma, material, notas, serie, habitación, biblioteca o espacio' %><br>
      <%= form.text_field :searched_text, value: @searched_text %>
      <%= form.submit "Buscar" %>
    <% end %>
    <%= yield %>
  </body>
</html>
